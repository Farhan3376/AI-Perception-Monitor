# Basic Computer Vision
opencv-python-headless>=4.8.0
numpy>=1.23.0

# Detection & Tracking
ultralytics>=8.0.0
# Optional tracking dependency (falls back gracefully if missing)
deep-sort-realtime>=1.3.1

# API & Web
fastapi>=0.103.0
uvicorn[standard]>=0.23.0
pydantic>=2.3.0
pydantic-settings>=2.0.0

# Database
motor>=3.3.0
# pymongo is a dependency of motor, but explicitly listed just in case
pymongo>=4.5.0

# Notifications & Config
aiohttp>=3.8.0
python-dotenv>=1.0.0
